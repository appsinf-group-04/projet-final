<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Your Register Page</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/public/css/output.css" rel="stylesheet">
  </head>
  <body class="min-h-screen bg-gray-100">

    <%- include("../components/header.ejs") %>

    <div class="flex items-center justify-center min-h-screen">
      <form method="POST" action="/auth/register" class="w-full max-w-md p-8 bg-white rounded-lg shadow-md">
        <div class="mb-4">
          <% if (errors && errors.some(e => e.path=="email")) { %>
            <p class="text-red-500"><%= errors.find(e => e.path=="email").message %></p>
          <% } %>

          <label for="email" class="block text-sm font-medium text-gray-700">Email</label> 
          <input type="email" name="email" id="email" value="<%= formData ? formData['email'] : '' %>" class="w-full p-2 mt-1 border rounded-md">
        </div>

        <div class="mb-4">
          <% if (errors && errors.some(e => e.path=="name")) { %>
            <p class="text-red-500"><%= errors.find(e => e.path=="name").message %></p>
          <% } %>

          <label for="name" class="block text-sm font-medium text-gray-700">Name</label> 
          <input type="text" name="name" id="name" class="w-full p-2 mt-1 border rounded-md" value="<%= formData ? formData['name'] : '' %>">
        </div>

        <div class="mb-4">
          <% if (errors && errors.some(e => e.path=="password")) { %>
            <p class="text-red-500"><%= errors.find(e => e.path=="password").message %></p>
          <% } %>

          <label for="password" class="block text-sm font-medium text-gray-700">Password</label> 
          <input type="password" name="password" id="password" class="w-full p-2 mt-1 border rounded-md">
        </div>

        <div class="mb-4">
          <% if (errors && errors.some(e => e.path=="phone")) { %>
            <p class="text-red-500"><%= errors.find(e => e.path=="phone").message %></p>
          <% } %>

          <label for="phone" class="block text-sm font-medium text-gray-700">Phone</label> 
          <input type="tel" name="phone" id="phone" class="w-full p-2 mt-1 border rounded-md" value="<%= formData ? formData['phone'] : '' %>">
        </div>

        <button type="submit" class="w-full px-4 py-2 text-white bg-blue-500 rounded-md hover:bg-blue-600">Register</button>
      </form>
    </div>
  </body>
</html>
